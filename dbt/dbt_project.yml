name: job_tracker
version: '1.0.0'
config-version: 2

profile: job_tracker

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  job_tracker:
    staging:
      +materialized: view
    marts:
      +materialized: table
